# Bootstrap connection for a private Sandpi deployment. UI-managed connection
# secrets will move to encrypted server-side storage; they are never browser state.
SANDBOX0_API_KEY=
SANDBOX0_API_HOST=https://api.sandbox0.ai
SANDPI_DATA_MODE=mock
